1. For each index in the input list:
    1. Set a j variable to the current index
    2. While j > 0 and the element at index j-1 is greater than the element at index j:
        1. Swap the elements at indices j and j-1
        2. Decrement j by 1
2. Return the list
